name: FootballPoints Robot Monitor (Manual Fixtures)

on:
  workflow_dispatch: {}

jobs:
  manual-mode:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm manual fixture mode
        run: |
          echo "FootballPoints is using MANUAL fixture management."
          echo "No football API key is required."
          echo "Admins add and update real fixtures from the FootballPoints Admin dashboard."
