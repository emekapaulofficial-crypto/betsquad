/* Superseded: sign-in navigation + auth-state sync is now handled exclusively by
   auth_fix.js, which removed the multi-file signIn-wrapping race this file used to
   contribute to. Kept as a harmless no-op so the existing <script> tag in index.html
   doesn't 404. Safe to delete this file (and its script tag) later. */
