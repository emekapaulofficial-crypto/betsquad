(function(){
function esc(x){return String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
async function adminSupport(){if(!state.user)return alert('Admin access required.');const {data:isAdmin,error:ae}=await supabase.rpc('is_admin');if(ae||!isAdmin)return alert('Admin access required.');let modal=document.getElementById('fpAdminSupportModal');if(!modal){modal=document.createElement('div');modal.id='fpAdminSupportModal';modal.style='position:fixed;inset:0;z-index:10002;background:#0009;display:flex;align-items:flex-end;justify-content:center;padding:16px';document.body.appendChild(modal)}modal.innerHTML='<div style="width:min(760px,100%);max-height:88vh;overflow:auto;background:#0d1b2e;color:#edf5ff;border:1px solid #28415e;border-radius:18px;padding:20px"><button style="float:right;background:transparent;color:#fff;border:0;font-size:24px" onclick="this.closest(\'#fpAdminSupportModal\').remove()">×</button><h2>💬 Customer Care Inbox</h2><div id="fpAdminSupportList">Loading...</div></div>';const {data:threads,error}=await supabase.from('support_threads').select('id,user_id,subject,status,created_at,profiles(display_name,email)').order('created_at',{ascending:false}).limit(100);if(error)return alert(error.message);document.getElementById('fpAdminSupportList').innerHTML=(threads||[]).map(t=>`<div class="panel" style="margin-bottom:10px"><b>${esc(t.subject)}</b><div class="small">${esc(t.profiles?.display_name||t.profiles?.email||t.user_id)} • ${esc(t.status)} • ${new Date(t.created_at).toLocaleString()}</div><button class="secondary" onclick="adminOpenSupport('${t.id}')">Open conversation</button></div>`).join('')||'<p class="muted">No support conversations.</p>'}
async function adminOpenSupport(id){const {data:t,error:te}=await supabase.from('support_threads').select('id,subject,status,user_id,profiles(display_name,email)').eq('id',id).single();if(te)return alert(te.message);const {data:msgs,error}=await supabase.from('support_messages').select('sender_id,message,created_at').eq('thread_id',id).order('created_at');if(error)return alert(error.message);document.getElementById('fpAdminSupportList').innerHTML=`<button class="secondary" onclick="adminSupport()">← Back</button><h3>${esc(t.subject)}</h3><p class="muted">${esc(t.profiles?.display_name||t.profiles?.email||t.user_id)}</p><div class="panel">${(msgs||[]).map(m=>`<div class="row"><span>${esc(m.message)}<br><small>${new Date(m.created_at).toLocaleString()}</small></span></div>`).join('')||'<p>No messages.</p>'}</div><textarea id="adminSupportReply" rows="4" placeholder="Reply to customer..."></textarea><button class="primary" onclick="adminReplySupport('${id}')">Send reply</button> <button class="secondary" onclick="adminCloseSupport('${id}')">Close ticket</button>`}
async function adminReplySupport(id){const message=document.getElementById('adminSupportReply')?.value.trim();if(!message)return alert('Write a reply.');const {error}=await supabase.from('support_messages').insert({thread_id:id,sender_id:state.user.id,message});if(error)return alert(error.message);await supabase.from('support_threads').update({status:'open',updated_at:new Date().toISOString()}).eq('id',id);adminOpenSupport(id)}
async function adminCloseSupport(id){const {error}=await supabase.from('support_threads').update({status:'closed',updated_at:new Date().toISOString()}).eq('id',id);if(error)return alert(error.message);adminSupport()}
window.adminSupport=adminSupport;window.adminOpenSupport=adminOpenSupport;window.adminReplySupport=adminReplySupport;window.adminCloseSupport=adminCloseSupport;
})();
